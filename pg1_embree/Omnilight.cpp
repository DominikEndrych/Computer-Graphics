#include "stdafx.h"
#include "Omnilight.h"

Omnilight::Omnilight() 
{
	position = Vector3(150, 140, 230);
	//position = Vector3(175, -140, 130);		// Same as camera position
	intensity = Vector3(1, 1, 1);
}
